# This project intentionally has NO third-party dependencies.
# Everything runs on the Python 3.9+ standard library.
#
# Optional, for development only:
#   pytest      # the test suite also runs under the stdlib: python -m unittest discover -s tests
